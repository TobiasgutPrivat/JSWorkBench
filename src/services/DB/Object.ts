export default class JSObject {
    id: number; //unique identifier for the object
    class: string; //the objects prototype
    data: string; //serialized data in Json format

    constructor(object: any) {
        //TODO
    }
}